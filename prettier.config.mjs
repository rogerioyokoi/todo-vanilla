/** @type {{singleQuote: boolean, trailingComma: string, tabWidth: number, semi: boolean}} */
const config = {
  trailingComma: 'all',
  tabWidth: 2,
  semi: true,
  singleQuote: true,
};

export default config;
